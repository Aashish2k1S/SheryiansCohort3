// ### Intermediate

// Add one username at beginning of array.

// **Hint:** Use `unshift()`

// ### Hard

// Insert multiple elements at beginning without replacing existing ones.

// **Hint:** Multiple arguments

let arr = [20, 30, 11, 22, 33, 99, 88, 77];

arr.unshift(10);
console.log(arr);

for (let a = 1; a < 5; a++) arr.unshift(a);
console.log(arr);
