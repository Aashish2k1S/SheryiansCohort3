// Check whether `"email"` exists inside an object or not.

// Hint:

// Use:

// ```jsx
// in
// ```

const user = {
    name: "Anubhav",
    age: 20
}

console.log(Object.keys(user).includes("email"));
// console.log("email" in user);