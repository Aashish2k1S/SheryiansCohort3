// Convert this object into an array of key-value pairs.
// ```jsx
const user = {
  name: "Aman",
  age: 21
}
// ```

// Hint:
// Use:
// ```jsx
// Object.entries()
// ```

console.log(Object.entries(user));